export const theme = {

    // ── Lavender (primary) ──────────────────────────────
    lavenderBg:       '#EDE9FD',   // page backgrounds, card fills
    lavenderSurface:  '#F5F3FF',   // header, sidebar surface
    lavenderBorder:   '#DDD8FA',   // borders, dividers
    lavenderMuted:    '#AFA9EC',   // secondary text, muted elements
    lavenderAccent:   '#7F77DD',   // primary buttons, active states
    lavenderDark:     '#534AB7',   // hover states, dark accents
    lavenderDeep:     '#26215C',   // headings, strong text

    // ── Rose (secondary) ────────────────────────────────
    roseBg:           '#FCE7F3',   // score rings, success states
    roseMuted:        '#ED93B1',   // muted rose elements
    roseAccent:       '#D4537E',   // feedback badges, highlights
    roseDark:         '#993556',   // rose text on light backgrounds

    // ── Neutrals ────────────────────────────────────────
    white:            '#FFFFFF',
    pageBg:           '#F5F0FF',   // outermost app background
    textPrimary:      '#26215C',   // all main text
    textSecondary:    '#534AB7',   // secondary text, labels
    textMuted:        '#8B87B8',   // hints, placeholders

    // ── Semantic ────────────────────────────────────────
    good:             '#7F77DD',   // score >= 75
    warning:          '#EF9F27',   // score >= 50
    bad:              '#E24B4A',   // score < 50
}